//example cat README | grep sometext
function parseCommand(text)
{
   //find if bar, if not output to command line
   //determine if file is a text file or link, dir won't work
   //get to "contents" of  file
   //send content to another function

}

function readContents(contents)
{
   //check if the contents contains the word we are looking for
   //if so output that line
   //if not, return default grep functionality when not found
}
